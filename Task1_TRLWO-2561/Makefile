RELEASE_TARGETS = rresolv
RELEASE_PREREQ  = $(addsuffix .c, $(RELEASE_TARGETS))
RELEASE_CFLAGS  = -std=c11 -Wall -pedantic -D_POSIX_C_SOURCE=200112L

CLEANUP_TARGETS  = $(RELEASE_TARGETS)

$(RELEASE_TARGETS): $(RELEASE_PREREQ)
	$(CC) -o $@ $^ $(RELEASE_CFLAGS)

clean:
	rm $(CLEANUP_TARGETS)